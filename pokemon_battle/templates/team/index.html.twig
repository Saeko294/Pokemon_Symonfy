{% extends 'base.html.twig' %}

{% block title %}Team index{% endblock %}

{% block body %}
    <h1 style="width:1200px; margin:0 auto; margin-top: 3%">Liste des équipes</h1>

    <div style="width:1200px; margin:0 auto; margin-top: 1%" align="center" class="jumbotron" >
    <table class="table table-hover">
        <thead>
            <tr class="table-success">
                <th scope="row">Identifiant</th>
                <th>Surnom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr class="table-success">
                <th scope="row">{{ team.id }}</th>
                <th>{{ team.NickName }}</th>
                <td>
                    <a class="btn btn-info" href="{{ path('team_show', {'id': team.id}) }}">show</a>
                    <a class="btn btn-warning" href="{{ path('team_edit', {'id': team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('team_new') }}">Create new</a>
    </div>
{% endblock %}
